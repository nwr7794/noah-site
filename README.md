# Noah Site

A lightweight personal website for Noah, built as a static HTML/CSS/JavaScript project.

## Local development

Open `index.html` in a browser, or serve the repository root with any static file server.

## Checks

```bash
npm run validate
```
